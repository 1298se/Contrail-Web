export enum Routes {
    ROOT = "/",
    LOGIN = "/login",
    REGISTER = "/register",
    MAIN = "/app",
    FILES = "/app/files",
    FAVOURITES = "/app/favourites",
    SHARED = "/app/shared",
    TRASH = "/app/trash",
}
